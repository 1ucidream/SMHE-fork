# SMHE 库编译和使用指南

## 项目简介

SMHE (Secure Multi-Key Homomorphic Encryption) 是一个支持定点运算的安全多密钥同态加密库。该库基于 HEAAN 库，扩展以支持多密钥 CKKS/BFV 加密。

- **语言**: C++
- **依赖库**: NTL (Number Theory Library)、GMP (GNU Multiple Precision)
- **目标平台**: Linux/Ubuntu 18.04+ (原生支持)、Windows (需特殊配置)

---

## ⚡ Windows 快速入门 (5分钟)

如果你在 Windows 上开发，请按以下步骤操作：

### 第1步: 安装 MSYS2

1. 访问 https://www.msys2.org/
2. 下载最新的 MSYS2 安装程序 (e.g., `msys2-x86_64-*.exe`)
3. 运行安装程序，按默认选项安装到 `C:\msys64`

### 第2步: 打开正确的终端

⚠️ **重要**：安装完成后，**必须打开 MSYS2 MinGW 64-bit 终端，不能打开其他的！**

MSYS2 安装后会产生多个终端快捷方式，你需要选择**正确的**那个。

#### ✅ 正确的终端

打开以下**任意一个**：
- `MSYS2 MinGW 64-bit` (推荐)
- `MSYS2 MinGW x86_64`

在 `C:\msys64\` 文件夹中对应的可执行文件：
- `mingw64.exe` (推荐)

#### ❌ 错误的终端 (会导致 make 找不到)

不要打开：
- `MSYS2 MSYS` → 提示符显示 `MSYS ~`（你现在就在这个）
- `MSYS2 MinGW 32-bit` → 只适合 32 位编译
- `msys2.exe` → 这是通用 MSYS 环境，不包含 MinGW 工具

#### 如何打开正确的终端

**方式 1: 开始菜单 (最简单)**
1. 点击 Windows 开始菜单
2. 搜索: `MSYS2 MinGW 64-bit` (注意关键字是 "MinGW 64-bit")
3. 点击找到的程序

**方式 2: 文件浏览器**
1. 打开文件浏览器
2. 导航到 `C:\msys64\`
3. 找到 `mingw64.exe`，双击打开（不是 `msys2.exe`）

**方式 3: 运行对话框**
1. 按 `Win + R`
2. 输入: `C:\msys64\mingw64.exe` (不是 `C:\msys64\msys2.exe`)
3. 按 Enter

✅ **正确的终端提示符**看起来像这样：
```
varerleet@DESKTOP-XXXXX MINGW64 ~
$ 
```

注意：关键是 `MINGW64` 而不是 `MSYS`

### ⚠️ 关键问题：Make 包可能损坏

如果您已按照步骤操作但 `make --version` 仍然失败，且 `ls -l /mingw64/bin/make` 显示文件不存在，说明 make 包可能**损坏或安装不完整**。

**快速解决方案**：
```bash
# 完全卸载并重新安装
pacman -R mingw-w64-x86_64-make
pacman -S mingw-w64-x86_64-make
```

或者安装包含 make 的完整开发工具：
```bash
pacman -S base-devel
```

### 第3步: 在 MSYS2 中安装工具

现在你在正确的 MSYS2 MinGW 64-bit 终端中，可以运行 `pacman` 命令了：

**第一次更新（可能需要重启终端）**：
```bash
pacman -Syu
```

如果上面的命令要求关闭终端，按 Ctrl+C 或关闭窗口，重新打开 **MSYS2 MinGW 64-bit** 终端。

**安装工具和库**：
```bash
pacman -S mingw-w64-x86_64-gcc
pacman -S mingw-w64-x86_64-make
pacman -S mingw-w64-x86_64-gmp
pacman -S mingw-w64-x86_64-ntl
```

⚠️ **如果 make 安装出现问题**，可以尝试：
```bash
pacman -S make
```

或者安装完整的开发工具：
```bash
pacman -S base-devel
```

### 第4步: 验证安装

在 MINGW64 终端中逐个运行以下命令：

```bash
g++ --version
```
预期输出: 显示 gcc 版本号（如 `g++ (GCC) 13.1.0`）

```bash
make --version
```
预期输出: 显示 make 版本号（如 `GNU Make 4.4`）

```bash
gmp-config --version
```
预期输出: 显示 GMP 版本号

```bash
ntl-config --version
```
预期输出: 显示 NTL 版本号

⚠️ **如果 make 仍然不可用**，请参考下方的"make 安装故障排查"部分

### 第5步: 编译 SMHE

现在所有依赖都已安装，可以编译 SMHE 库了！

**第一次编译（编译静态库）**：
```bash
# 进入库编译目录
cd /path/to/SMHE/MFHE-1.1/MFHE/lib

# 编译
make all
```

**预期输出**：
- 开始显示编译过程
- 最后显示 `libMFHE.a` 生成成功
- 没有错误信息

**第二次编译（编译和运行测试）**：
```bash
# 进入测试目录
cd ../run

# 编译测试程序
make

# 运行测试
./TestMFHE
```

**预期结果**：
- 看到 4 个测试函数的执行结果
- 显示加密/解密是否成功

就这样！✅

---

## 前置依赖安装

### 在 Linux/Ubuntu 上

```bash
# 安装基础工具
sudo apt-get update
sudo apt-get install build-essential

# 安装 GMP
sudo apt-get install libgmp-dev

# 安装 NTL (推荐版本 10.4.0 或更新)
# 方式1: 通过包管理器 (可能不是 10.4.0)
sudo apt-get install libntl-dev

# 方式2: 从源码编译 (推荐，确保版本正确)
cd /tmp
wget https://www.shoup.net/ntl/ntl-10.4.0.tar.gz
tar -xzf ntl-10.4.0.tar.gz
cd ntl-10.4.0/src
./configure
make
sudo make install

# 安装 pThread (通常已包含在 build-essential 中)
sudo apt-get install libpthread-stubs0-dev
```

### 在 Windows 上

**重要**: 首先选择开发环境，然后安装对应的工具。

#### ✅ 推荐: 使用 MSYS2

MSYS2 是一个在 Windows 上提供 Unix-like 环境的最简单方案。

**步骤1: 安装 MSYS2**
1. 访问 https://www.msys2.org/
2. 下载并安装 MSYS2 最新版本
3. 安装完成后，**打开 "MSYS2 MinGW 64-bit"** 终端窗口（不是 PowerShell）

**步骤2: 在 MSYS2 中安装依赖**

在 MSYS2 MinGW 64-bit 终端中执行：

```bash
# 更新包管理器
pacman -Syu

# 安装编译工具
pacman -S mingw-w64-x86_64-gcc
pacman -S mingw-w64-x86_64-make

# 安装数学库
pacman -S mingw-w64-x86_64-gmp
pacman -S mingw-w64-x86_64-ntl
```

**步骤3: 验证安装**

```bash
g++ --version
make --version
gmp-config --version
```

如果都能显示版本号，说明安装成功！

#### 🔄 备选方案 1: 使用 Git Bash

如果你已安装 Git for Windows：
1. 右键打开 **Git Bash** (不是 PowerShell)
2. 手动编译 NTL 和 GMP (参见下方)

#### 🔄 备选方案 2: 在 Windows 上安装 Make

如果你坚持在 PowerShell 或 CMD 中工作，可以单独安装 make：

```powershell
# 使用 Chocolatey (需要先安装 Chocolatey)
choco install make

# 或使用 Scoop
scoop install make
```

安装后重启 PowerShell。

#### 手动从源码编译 NTL 和 GMP (在 MSYS2 中)

```bash
# 1. 下载 GMP
cd /tmp
wget https://gmplib.org/download/gmp/gmp-6.2.1.tar.xz
tar -xf gmp-6.2.1.tar.xz
cd gmp-6.2.1

./configure --prefix=/mingw64
make
make install

# 2. 下载并编译 NTL
cd /tmp
wget https://www.shoup.net/ntl/ntl-10.4.0.tar.gz
tar -xzf ntl-10.4.0.tar.gz
cd ntl-10.4.0/src

./configure NTL_GMP_LIP=on --prefix=/mingw64
make
make install
```

---

## 编译步骤

### 前提条件

**在 Windows 上，你需要选择以下方案之一：**

#### 方案 A: 使用 MSYS2 (推荐) ⭐

MSYS2 提供了完整的 Unix-like 环境和 `make` 工具。

1. **安装 MSYS2**: https://www.msys2.org/
2. **打开 MSYS2 MinGW 64-bit 终端** (不是 PowerShell)
3. 在 MSYS2 中安装工具：
   ```bash
   pacman -S mingw-w64-x86_64-gcc
   pacman -S mingw-w64-x86_64-make
   pacman -S mingw-w64-x86_64-gmp
   pacman -S mingw-w64-x86_64-ntl
   ```
4. 验证：
   ```bash
   make --version
   g++ --version
   ```

#### 方案 B: 使用 Git Bash

1. 安装 Git for Windows (https://git-scm.com/download/win)
2. Git Bash 中也包含 `make` 和 Unix 工具
3. 右键打开 **Git Bash**，不是 PowerShell

#### 方案 C: 在 Windows 上安装 Make

如果你只想在 Windows PowerShell 或 CMD 中工作：

```powershell
# 使用 Chocolatey (如果已安装)
choco install make

# 或者使用 scoop
scoop install make
```

然后重启 PowerShell。

---

### Step 1: 编译静态库

**在 MSYS2 或 Git Bash 中执行：**

```bash
cd MFHE-1.1/MFHE/lib
cd C:/all-github-repositories/SMHE02/MFHE-1.1/MFHE/lib
make all
```

**在 Windows PowerShell 中执行 (安装 make 后)：**

```powershell
cd MFHE-1.1/MFHE/lib
make all
```

**预期结果**: 在 `lib` 目录中生成 `libMFHE.a` 或 `libMFHE.lib` 静态库文件

**常见错误排查**:

1. **错误**: `error: 'M_PI' was not declared in this scope`
   - **原因**: Windows MinGW 中 M_PI 常量未定义（C++ 标准差异）
   - **已修复文件**:
     - `src/Context.cpp` ✅
     - `src/EvaluatorUtils.cpp` ✅
   - **现象**: 编译时在这两个文件中报错
   - **解决**: 已在这些文件顶部添加 `#ifndef M_PI #define M_PI ...` 定义
   - **如果仍然出现**: 可能代码未更新，需要手动在这两个文件顶部添加上述定义


// Define M_PI for Windows MinGW compatibility
#ifndef M_PI
#define M_PI 3.14159265358979323846
#endif

2. **错误**: `make: command not found`
   - **原因**: make 工具未安装或不在 PATH 中
   - **解决**: 参考前面的 "make 安装故障排查" 部分

3. **错误**: `找不到 ntl/ntl.h` 或 `找不到 -lntl/-lgmp`
   - **原因**: NTL/GMP 库路径未正确配置
   - **解决**: 确保已安装 NTL 和 GMP，检查 include/lib 路径

### Step 2: 运行测试 (可选)

**在 MSYS2 或 Git Bash 中执行：**

```bash
cd ../run
cd C:/all-github-repositories/SMHE/MFHE-1.1/MFHE/run
make clean
make
./TestMFHE
```

**预期结果**: 运行四个测试函数，每个测试的输出如下：

#### 测试 1: testGadgetDecompose
```
========================================
TEST 1: testGadgetDecompose
========================================
Gadget Decompose: Verification successful: <h, g> = ... (mod ...)
```

#### 测试 2: testGadgetEncryption
```
========================================
TEST 2: testGadgetEncryption
========================================
---------------------
madd: 0 :(real_part,imag_part)
dadd: 0 :(decrypted_real,decrypted_imag)
eadd: 0 :(error_real,error_imag)
---------------------
[更多槽位的数据...]
```

#### 测试 3: testExternalProduct
```
========================================
TEST 3: testExternalProduct
========================================
External product: Verification successful
[madd/dadd/eadd 输出...]
```

#### 测试 4: testSecureMFHE
```
========================================
TEST 4: testSecureMFHE
========================================
!!! START TEST Secure MKFHE MFScheme (MFHE) !!!
[大量的计时输出和 madd/dadd/eadd 数据...]
```

**最终输出**:
```
========================================
ALL TESTS COMPLETED SUCCESSFULLY!
========================================
```

---

## 📊 理解测试输出

当您看到类似以下的输出时：

```
madd: 8191 :(1.41026,1.59554)
dadd: 8191 :(1.40997,1.59542)
eadd: 8191 :(0.000288551,0.000119171)
```

这表示：
- **m** 前缀: 明文 (plaintext) 计算的结果
- **d** 前缀: 同态加密后的结果（密文加法后解密）
- **e** 前缀: 误差（precision loss）

这些数据来自 `StringUtils::showcompare()` 函数，用于验证同态运算的正确性。

---

## ⚠️ 诊断: 我在正确的终端中吗?

### Make 安装故障排查

如果运行 `make --version` 仍然显示 `-bash: make: command not found`，请按以下步骤操作：

#### 症状检查

首先确认您遇到的是哪种情况：

```bash
# 检查 make 包是否已安装
pacman -Q | grep make
```

- **情况 A**：显示 `mingw-w64-x86_64-make 4.4.1-3` → 包已安装，但文件缺失（本页适用）
- **情况 B**：无输出 → 包未安装，需要装 (执行下面的安装步骤)

#### 🔧 解决方案（针对情况 A：包已安装但文件缺失）

这表示 make 包**损坏或安装不完整**。需要彻底卸载并重新安装：

**步骤 1: 卸载损坏的包**
```bash
pacman -R mingw-w64-x86_64-make --noconfirm
```

**步骤 2: 清理缓存**
```bash
pacman -Scc --noconfirm
```

**步骤 3: 重新同步包数据库**
```bash
pacman -Sy
```

**步骤 4: 重新安装 make**

*选项 A*: 只安装 make
```bash
pacman -S mingw-w64-x86_64-make
```

*选项 B*: 安装完整的开发工具包（包含 make 和其他必要工具，推荐）
```bash
pacman -S base-devel
```

**步骤 5: 验证安装**
```bash
# 检查文件是否存在
ls -l /mingw64/bin/make

# 测试 make 命令
make --version
```

#### 更激进的解决方案（如果上面不行）

如果重新安装还是不行，尝试：

```bash
# 完整的系统升级
pacman -Syu --noconfirm

# 强制重新安装
pacman -S --force mingw-w64-x86_64-make

# 或者安装所有开发工具
pacman -S base-devel mingw-w64-x86_64-toolchain
```

#### 验证最终结果

```bash
# 应该显示 make 的路径
which make

# 应该显示版本号
make --version

# 应该看到 /mingw64/bin/make
ls -l /mingw64/bin/make
```

**成功标志**：
- `which make` 显示 `/mingw64/bin/make`
- `make --version` 显示版本号（如 `GNU Make 4.4`）
- `ls -l` 显示文件存在

---

### 常见问题与解决

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| `make: command not found` 且包已安装 | 包损坏 | 卸载并重新安装 |
| `make: command not found` 且包未安装 | 没有安装 | 运行 `pacman -S mingw-w64-x86_64-make` |
| 文件存在但找不到命令 | PATH 设置问题 | 运行 `export PATH=/mingw64/bin:$PATH` |
| 重新安装后仍然失败 | 磁盘空间或权限问题 | 清理缓存：`pacman -Scc --noconfirm` |

---

### 常见问题

✅ **正确的终端** (MSYS2 MinGW 64-bit):
- 提示符显示: `varerleet@DESKTOP-XXXXX MINGW64 ~` ← **关键是 MINGW64**
- 终端标题栏显示: "MSYS2 MinGW x86_64"
- 命令有效: `pacman --version` (能显示版本号)
- 命令有效: `g++ --version` (能显示版本号)
- 命令有效: `make --version` (能显示版本号)

❌ **错误的终端** (MSYS2 MSYS):
- 提示符显示: `user@DESKTOP-XXXXX MSYS ~` ← **错！应该是 MINGW64**
- 终端标题栏显示: "MSYS2"
- 命令失败: `pacman: command not found`
- 命令失败: `make: command not found`

### 立即解决方案

**如果仍然在 MSYS 终端中：**

关闭当前的 MSYS 终端，打开正确的 MinGW 64-bit 终端：

#### 方式 1: 开始菜单 (最简单) ⭐
1. 点击 Windows 开始菜单
2. 搜索: `mingw 64` (搜索 "mingw 64-bit" 也可以)
3. 点击找到的 **"MSYS2 MinGW 64-bit"** 程序
4. 等待终端启动
5. 验证提示符中显示 `MINGW64`

#### 方式 2: 直接运行
1. 按 `Win + R` 打开运行对话框
2. 输入: `C:\msys64\mingw64.exe`（**注意是 mingw64.exe，不是 msys2.exe**）
3. 按 Enter

#### 方式 3: 文件浏览器
1. 打开文件浏览器
2. 导航到 `C:\msys64\`
3. 找到 `mingw64.exe`（**不是 msys2.exe，也不是 mingw32.exe**）
4. 双击打开

### 验证你打开了正确的终端

打开新终端后，输入：

```bash
echo $MSYSTEM
```

- 如果显示 `MINGW64` ✅ → 正确！
- 如果显示 `MSYS` ❌ → 错了，关闭这个终端，用方式1-3重新打开

---

## ⚠️ 重要提示

**不要在原生 Windows PowerShell 或 CMD 中直接运行 `make` 命令！**

你有三个选择：
1. **推荐** ⭐: 使用 MSYS2 MinGW 64-bit 终端
2. 使用 Git Bash 
3. 在 Windows 上单独安装 make 工具

---

## ✅ 编译验证清单

如果您已按照上面的步骤操作，现在应该有：

- ✅ MSYS2 MinGW 64-bit 终端打开
- ✅ `make --version` 显示 `GNU Make 4.4.1` 或更新版本
- ✅ `g++ --version` 显示编译器版本
- ✅ 已安装 GMP 和 NTL 库

### 下一步：编译 SMHE

在 MINGW64 终端中执行：

```bash
# 1. 导航到项目目录
cd c:/all-github-repositories/SMHE/MFHE-1.1/MFHE/lib

# 2. 编译静态库
make all

# 3. 编译完成后进入测试目录
cd ../run

# 4. 编译测试程序
make

# 5. 运行测试
./TestMFHE
```

**Windows 路径说明**：
- 如果项目在其他位置，请替换上面的路径
- MINGW64 中使用正斜杠 `/` 而不是反斜杠 `\`
- 或者使用混合格式：`c:/path/to/SMHE/...`

---

当前 `run.cpp` 中有以下测试函数:

### 1. `testGadgetDecompose()`
- **功能**: 测试 Gadget 分解
- **参数**: `logN, logQ, logp, logSlots, num_parties`
- **推荐参数**: `13, 65, 30, 0, 2`

### 2. `testGadgetEncryption()`
- **功能**: 测试 Gadget 加密和解密
- **参数**: `logN, logQ, logp, logSlots, num_parties`
- **推荐参数**: `13, 65, 30, 0, 2`

### 3. `testExternalProduct()`
- **功能**: 测试外部乘积
- **参数**: `logN, logQ, logp, logSlots`
- **推荐参数**: `13, 65, 30, 0`

### 4. `testSecureMFHE()`
- **功能**: 测试完整的安全多密钥同态加密
- **参数**: `logN, logQ, logp, logSlots, num_parties`
- **推荐参数**: `14, 256, 60, 13, 2`

---

## 参数说明

- **logN**: 环的维度 (2^logN)，必须 > 8，通常 13-14
- **logQ**: 模数大小 (log2)，通常 65-256
- **logp**: 明文编码的位数，通常 30-60
- **logSlots**: 并行槽数 (log2)，必须 < logN，通常 0-13
- **num_parties**: 参与方数量，通常 2-10

---

## 项目结构详解

```
MFHE-1.1/MFHE/
├── src/                    # 核心源代码
│   ├── MFHE.h             # 主头文件
│   ├── MFScheme.h/cpp     # SMHE 方案实现
│   ├── MFCiphertext.h/cpp # 密文类
│   ├── MFPlaintext.h/cpp  # 明文类
│   ├── Context.h/cpp      # 上下文管理
│   └── ...                # 其他工具类
├── lib/                    # 静态库编译目录
│   ├── makefile           # 库编译配置
│   └── src/               # 生成的对象文件目录
└── run/                    # 测试程序目录
    ├── makefile           # 测试编译配置
    └── run.cpp            # 测试代码
```

---

## 如何集成到自己的项目中

### 1. 基本使用

```cpp
#include "MFHE.h"
using namespace std;

int main() {
    // 初始化参数
    long logN = 13;
    long logQ = 65;
    long logp = 30;
    long logSlots = 0;
    
    // 创建上下文
    Context context(logN, logQ, logp);
    
    // 创建方案实例
    MFScheme scheme(context, logSlots);
    
    // ... 进行加密/解密操作
    
    return 0;
}
```

### 2. 编译自己的程序

```bash
g++ myprogram.cpp -o myprogram \
    -L/path/to/MFHE/lib libMFHE.a \
    -lntl -lgmp -lm -O2 \
    -std=c++11 -pthread \
    -I/path/to/MFHE/src
```

---

## 性能优化建议

1. **编译优化标志**: 使用 `-O2` 或 `-O3` (已在 makefile 中启用)
2. **多线程**: 使用 `-pthread` 编译标志
3. **参数选择**:
   - 更大的 logN 提供更高的安全性但性能下降
   - 更大的 logQ 提供更高精度但性能下降
   - logSlots 控制并行度

---

## 常见问题

### Q1: 编译时找不到 NTL/GMP
**A**: 确保：
- NTL 已正确安装
- 设置了正确的 `LD_LIBRARY_PATH` 和 `CPLUS_INCLUDE_PATH`
- 在 makefile 中检查 `-L` 和 `-I` 标志

### Q2: 运行测试时出错
**A**: 
- 检查是否正确编译了 `libMFHE.a`
- 确保参数设置合理 (logN > 8, logSlots < logN)
- 增加参数中的 logQ 可能解决某些数值问题

### Q3: Windows 上如何编译?
**A**: 使用 MSYS2 MinGW 环境，安装相应的二进制包，或从源码编译 NTL 和 GMP

### Q4: 如何生成共享库 (.so/.dll)?
**A**: 修改 lib/makefile，将静态库编译改为动态库:

```makefile
libMFHE.so: $(OBJS)
	g++ -shared -fPIC -o libMFHE.so $(OBJS) -lntl -lgmp -lm
```

---

## 参考资源

- **NTL 文档**: http://www.shoup.net/ntl/
- **SMHE 论文**: https://eprint.iacr.org/2016/421.pdf
- **GMP 文档**: https://gmplib.org/
- **项目 GitHub**: https://github.com/JiahuiWu2022/SMHE

---

## 许可证

本项目采用 Creative Commons Attribution-NonCommercial 3.0 Unported License

